version: '3.9'

services:
  api:
    container_name: ph-nest-api

    build:
      context: .

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    environment:
      NODE_ENV: development

    command: npm run start:dev